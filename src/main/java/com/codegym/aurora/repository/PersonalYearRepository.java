package com.codegym.aurora.repository;

import com.codegym.aurora.entity.PersonalYear;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PersonalYearRepository extends JpaRepository<PersonalYear, Long> {
}
